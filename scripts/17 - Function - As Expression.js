/*
    Anonymous Function -> Function yang tidak memiliki nama,
*/

const volumeBalok = function (panjang, lebar, tinggi) {
    const volume  = lebar * panjang * tinggi;
    return volume;
}

volumeBalok(4, 3, 2);



//#region STUDY CASE

// NOTHING

//#endregion